import 'package:flutter/material.dart';

class OpersScreen extends StatelessWidget {
  const OpersScreen({Key key}) : super(key: key);

@override
  Widget build(BuildContext context) {
    return new Center(
      child: Text('OpersScreen Screen', style: new TextStyle(fontSize: 20.0)),
    ); 
  }
}